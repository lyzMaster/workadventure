import type { BaseTranslation } from "../i18n-types";

const menu: BaseTranslation["menu"] = {
    "shortcuts": {
        "moveUp": "Move Up",
        "moveDown": "Move Down",
        "moveLeft": "Move Left",
        "moveRight": "Move Right",
        "speedUp": "Run",
        "interact": "Interact",
        "follow": "Follow"
    }
};

export default menu;
